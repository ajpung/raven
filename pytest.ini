[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --cov=raven --cov-report=term-missing
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests